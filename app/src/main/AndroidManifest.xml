<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodAppDemo"
        tools:targetApi="m">
        <activity android:name="food.app.activity.Activities.FoodDetailActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="food.app.activity.Activities.CartActivity"
            android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name="food.app.activity.Activities.NoOrderActivity"  android:exported="true"/>
        <activity android:name="food.app.activity.Activities.NoHistoryActivity"  android:exported="true"/>
        <activity android:name="food.app.activity.Activities.DashboardActivity" android:exported="true"/>
        <activity android:name="food.app.activity.Activities.ModifyUserActivity" android:exported="true"/>
        <activity android:name="food.app.activity.Activities.LoginActivity" />
        <activity android:name="food.app.activity.Activities.GetStartedActivity" />
        <activity android:name="food.app.activity.Activities.ProfileActivity" android:exported="true"/>
        <activity android:name="food.app.activity.Activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>